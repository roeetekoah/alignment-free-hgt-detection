Transient and historical run outputs live under this directory.

- `legacy_runs/` contains old ad-hoc reruns that were previously under `src/`:
  - `out/`
  - `rerun_pruned/`
  - `rerun_results/`
  - `rerun_results_no_bw/`

Canonical reproducibility baselines remain in `golden/`.

Historical hackathon deliverables were moved to:
- `historical_results/results_hackathon_run/`
